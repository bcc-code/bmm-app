using BMM.Core.ViewModels;

namespace BMM.Core.Implementations.UI
{
    /// <summary>
    /// Right now it's only valid to be used by iOS. But we should change <see cref="QueueViewModel"/> on Android to use Dark style so that it's usable by both.
    /// </summary>
    public interface IDarkStyleOnIosViewModel
    {
    }

    public interface IDarkStyleOnAndroidViewModel
    {
    }
}